{
  "name": "defi-vault",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "ts-mocha -p tsconfig.json -t 1000000 tests/**/*.ts",
    "build": "npm --prefix frontend run build && node scripts/stage-site.cjs"
  },
  "dependencies": {
    "@coral-xyz/anchor": "0.31.1",
    "@solana/spl-token": "0.4.14"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.0",
    "@types/node": "^22.0.0",
    "mocha": "^11.0.0",
    "ts-mocha": "^11.0.0",
    "typescript": "~5.7.3"
  },
  "overrides": {
    "toml": "4.2.0",
    "serialize-javascript": "7.0.5"
  }
}
